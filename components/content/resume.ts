/** The public résumé, served from /public once a public-safe copy is there. The
 * copies in the earlier portfolios list a private phone number, which this site
 * leaves out on purpose, so neither is published; the download appears only when
 * a copy without it is added under this name. */
export const RESUME = { file: "Aadit-Praveen-Nath-Resume.pdf", href: "/Aadit-Praveen-Nath-Resume.pdf" } as const;
